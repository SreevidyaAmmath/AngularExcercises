import { AwardFilterPipe } from './award-filter.pipe';

describe('AwardFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AwardFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
